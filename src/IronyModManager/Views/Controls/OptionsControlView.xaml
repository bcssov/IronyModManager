<UserControl xmlns="https://github.com/avaloniaui"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d" d:DesignWidth="800" d:DesignHeight="450"
             xmlns:controls="clr-namespace:IronyModManager.Controls;assembly=IronyModManager"
             xmlns:html="clr-namespace:Avalonia.Controls.Html;assembly=Avalonia.HtmlRenderer"
             xmlns:colorPicker="clr-namespace:ThemeEditor.Controls.ColorPicker;assembly=ThemeEditor.Controls.ColorPicker"
             x:Class="IronyModManager.Views.Controls.OptionsControlView">
    <Panel>
        <Button Content="{Binding Options}" Command="{Binding OptionsCommand}" BorderThickness="0,0,0,0" Background="Transparent" Foreground="{DynamicResource IronyAccentBrush}" x:Name="openPopupButton" />
        <Popup PlacementMode="AnchorAndGravity" PlacementGravity="Bottom" VerticalOffset="15" IsOpen="{Binding IsOpen}" Topmost="True" ObeyScreenEdges="True" StaysOpen="False" x:Name="popup" PlacementTarget="{Binding #openPopupButton}">
            <Border x:Name="optionsSurface" BorderThickness="1" BorderBrush="{DynamicResource IronyButtonBorderBrush}" Background="{DynamicResource IronyBackgroundPopupBrush}">
                <Grid RowDefinitions="*,Auto">
                    <TabControl Grid.Row="0">
                        <TabControl.ItemsPanel>
                            <ItemsPanelTemplate>
                                <WrapPanel Orientation="Horizontal" />
                            </ItemsPanelTemplate>
                        </TabControl.ItemsPanel>
                        <TabItem>
                            <TabItem.Header><TextBlock Text="{Binding AppOptionsTitle}" TextWrapping="Wrap" TextAlignment="Center" /></TabItem.Header>
                            <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled" Padding="10">
                                <StackPanel>
                                    <Grid ColumnDefinitions="1*,1*" Margin="0,5"><TextBlock Text="{Binding NotificationPositionLabel}" Margin="0,5,0,0" VerticalAlignment="Center" TextWrapping="Wrap" /><controls:ComboBox Items="{Binding NotificationPositions}" SelectedItem="{Binding NotificationPosition}" MinHeight="25" MinWidth="80" Margin="0,5,0,0" MaxWidth="180" Grid.Column="1"><controls:ComboBox.ItemTemplate><DataTemplate><ComboBoxItem IsSelected="{Binding IsSelected}" Content="{Binding Name}" /></DataTemplate></controls:ComboBox.ItemTemplate></controls:ComboBox></Grid>
                                    <Border Margin="0,10,0,0" BorderThickness="1" BorderBrush="{DynamicResource IronyButtonBorderBrush}"><TextBlock Text="{Binding EditorTitle}" FontSize="14" HorizontalAlignment="Center" TextWrapping="Wrap" /></Border>
                                    <Grid ColumnDefinitions="3*,3*,Auto,30" RowDefinitions="Auto,Auto,Auto" Margin="0,5">
                                        <TextBlock Text="{Binding EditorExecutable}" Margin="0,5,0,0" Grid.Row="0" VerticalAlignment="Center" TextWrapping="Wrap" /><controls:TextBox IsReadOnly="True" Text="{Binding Editor.ExternalEditorLocation}" Margin="0,5,0,0" Grid.Column="1" Grid.Row="0" /><Button Content="{Binding Navigate}" Command="{Binding NavigateEditorCommand}" Margin="0,5,0,0" Grid.Column="2" Grid.Row="0" /><Button Content="{Binding Reset}" Command="{Binding ResetEditorExeCommand}" Margin="0,5,0,0" Grid.Column="3" Grid.Row="0" />
                                        <TextBlock Margin="0,5" Text="{Binding EditorArgs}" Grid.Row="1" VerticalAlignment="Center" TextWrapping="Wrap" /><controls:TextBox Margin="0,5" Text="{Binding Editor.ExternalEditorParameters}" Grid.Column="1" Grid.Row="1" Watermark="{Binding EditorArgsPlaceholder}" /><Button Margin="0,5" Content="{Binding Reset}" Command="{Binding ResetEditorArgsCommand}" Grid.Column="2" Grid.Row="1" Grid.ColumnSpan="2" />
                                        <Button Margin="0,5" Content="{Binding TestExternalEditorConfiguration}" Command="{Binding TestExternalEditorConfigurationCommand}" Grid.Row="2" Grid.ColumnSpan="4" MaxHeight="30" />
                                    </Grid>
                                </StackPanel>
                            </ScrollViewer>
                        </TabItem>
                        <TabItem IsVisible="{Binding ShowGameOptions}">
                            <TabItem.Header><TextBlock Text="{Binding GameOptions}" TextWrapping="Wrap" TextAlignment="Center" /></TabItem.Header>
                            <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled" Padding="10">
                                <StackPanel>
                                    <Grid ColumnDefinitions="3*,3*,Auto,30" RowDefinitions="Auto,Auto,Auto,Auto,Auto,Auto,Auto" Margin="0,5">
                                        <Button Content="{Binding AutoConfigure}" Command="{Binding AutoConfigureCommand}" Grid.Row="0" Grid.ColumnSpan="4" MaxHeight="30" />
                                        <TextBlock Text="{Binding GameExecutable}" Margin="0,5,0,0" Grid.Row="1" VerticalAlignment="Center" TextWrapping="Wrap" /><controls:TextBox IsReadOnly="True" Text="{Binding Game.ExecutableLocation}" Margin="0,5,0,0" Grid.Column="1" Grid.Row="1" /><Button Content="{Binding Navigate}" Command="{Binding NavigateExeCommand}" Margin="0,5,0,0" Grid.Column="2" Grid.Row="1" /><Button Content="{Binding Reset}" Command="{Binding ResetExeCommand}" Margin="0,5,0,0" Grid.Column="3" Grid.Row="1" />
                                        <TextBlock Margin="0,5" Text="{Binding GameArgs}" Grid.Row="2" VerticalAlignment="Center" TextWrapping="Wrap" /><controls:TextBox Margin="0,5" Text="{Binding Game.LaunchArguments}" Grid.Column="1" Grid.Row="2" /><Button Margin="0,5" Content="{Binding Reset}" Command="{Binding ResetArgsCommand}" Grid.Column="2" Grid.Row="2" Grid.ColumnSpan="2" />
                                        <TextBlock Text="{Binding UserDirectory}" Grid.Row="3" VerticalAlignment="Center" TextWrapping="Wrap" /><controls:TextBox IsReadOnly="True" Text="{Binding Game.UserDirectory}" Grid.Column="1" Grid.Row="3" /><Button Content="{Binding Navigate}" Command="{Binding NavigateDirectoryCommand}" Grid.Column="2" Grid.Row="3" /><Button Content="{Binding Reset}" Command="{Binding ResetDirectoryCommand}" Grid.Column="3" Grid.Row="3" />
                                        <TextBlock Text="{Binding CustomModPath}" Grid.Row="4" VerticalAlignment="Center" TextWrapping="Wrap" /><controls:TextBox IsReadOnly="True" Text="{Binding Game.CustomModDirectory}" Grid.Column="1" Grid.Row="4" /><Button Content="{Binding Navigate}" Command="{Binding NavigateCustomDirectoryCommand}" Grid.Column="2" Grid.Row="4" /><Button Content="{Binding Reset}" Command="{Binding ResetCustomDirectoryCommand}" Grid.Column="3" Grid.Row="4" />
                                        <CheckBox Margin="0,5,0,0" Content="{Binding RefreshDescriptors}" IsChecked="{Binding Game.RefreshDescriptors}" Grid.ColumnSpan="4" Grid.Row="5" /><CheckBox Margin="0,5,0,0" Content="{Binding CloseAppAfterGameLaunch}" IsChecked="{Binding Game.CloseAppAfterGameLaunch}" Grid.ColumnSpan="4" Grid.Row="6" />
                                    </Grid>
                                    <Border Margin="0,10,0,0" BorderThickness="1" BorderBrush="{DynamicResource IronyButtonBorderBrush}"><TextBlock Text="{Binding ModeMergeTitle}" FontSize="14" HorizontalAlignment="Center" TextWrapping="Wrap" /></Border>
                                    <Grid ColumnDefinitions="1*,1*,30" RowDefinitions="Auto,Auto" Margin="0,5"><TextBlock Grid.Row="0" Text="{Binding ModMergeTemplateTitle}" TextWrapping="WrapWithOverflow" Margin="0,5,0,0" /><controls:BoundClassBorder BoundClasses="{Binding ModMergeInvalidClass}" Grid.Row="0" Grid.Column="1" Margin="0,5,0,0"><controls:TextBox Text="{Binding ModeMergeTemplate}" Watermark="{Binding ModeMergeWatermark}" /></controls:BoundClassBorder><Button Content="{Binding Reset}" Command="{Binding ModMergeTemplateResetCommand}" Grid.Column="2" Grid.Row="0" Margin="0,5,0,0" /><TextBlock Grid.Row="1" Text="{Binding ModCollectionTemplateTitle}" TextWrapping="WrapWithOverflow" Margin="0,5" /><controls:BoundClassBorder BoundClasses="{Binding ModCollectionInvalidClass}" Grid.Row="1" Grid.Column="1" Margin="0,5"><controls:TextBox Text="{Binding ModCollectionTemplate}" Watermark="{Binding ModeMergeWatermark}" /></controls:BoundClassBorder><Button Content="{Binding Reset}" Command="{Binding ModCollectionTemplateResetCommand}" Grid.Column="2" Grid.Row="1" Margin="0,5" /></Grid>
                                </StackPanel>
                            </ScrollViewer>
                        </TabItem>
                        <TabItem IsVisible="{Binding GameLanguagesVisible}">
                            <TabItem.Header><TextBlock Text="{Binding ConflictSolverTitle}" TextWrapping="Wrap" TextAlignment="Center" /></TabItem.Header>
                            <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled" Padding="10"><StackPanel>
                                <TextBlock Text="{Binding AllowedLanguagesCaption}" VerticalAlignment="Center" TextWrapping="Wrap" Margin="0,5" /><ItemsRepeater Items="{Binding GameLanguages}" HorizontalAlignment="Stretch"><ItemsRepeater.Layout><UniformGridLayout Orientation="Horizontal" MinColumnSpacing="10" ItemsStretch="Fill" MinItemWidth="140" /></ItemsRepeater.Layout><ItemsRepeater.ItemTemplate><DataTemplate><CheckBox IsChecked="{Binding IsSelected}"><TextBlock Text="{Binding DisplayName}" TextWrapping="WrapWithOverflow" /></CheckBox></DataTemplate></ItemsRepeater.ItemTemplate></ItemsRepeater>
                                <TextBlock Text="{Binding ConflictSolverColorsTitle}" VerticalAlignment="Center" TextWrapping="Wrap" Margin="0,15,0,5" />
                                <Grid RowDefinitions="Auto,Auto,Auto,Auto" MaxWidth="360" HorizontalAlignment="Left">
                                    <Grid Grid.Column="0" Grid.Row="0" ColumnDefinitions="1*,100,Auto,30"><TextBlock Text="{Binding ConflictSolverDeletedLineColorText}" TextWrapping="WrapWithOverflow" Margin="0,5,0,0" /><StackPanel Grid.Column="2"><ToggleButton Name="PopupButtonDeleted" Background="Transparent" Width="100" Height="30" Margin="0,0,5,0"><Grid><Rectangle Width="100" Height="{Binding #PopupButtonDeleted.Bounds.Height}"><Rectangle.Fill><VisualBrush TileMode="Tile" SourceRect="0, 0, 16, 16" DestinationRect="0, 0, 16, 16"><VisualBrush.Visual><Path Data="M 0,0 L 0,8 L 8,8 L 8,0 Z M 8,8 L 8,16 L 16,16 L 16,8 Z" Fill="Gray" /></VisualBrush.Visual></VisualBrush></Rectangle.Fill></Rectangle><Rectangle Fill="{Binding Color.ConflictSolverDeletedLineBrush}" Width="200" Height="200" /></Grid></ToggleButton><Popup IsOpen="{Binding #PopupButtonDeleted.IsChecked, Mode=TwoWay}" StaysOpen="False" PlacementTarget="{Binding #PopupButtonDeleted}"><Border Background="{DynamicResource IronyBackgroundPopupBrush}" Padding="5" CornerRadius="3"><colorPicker:ColorPicker Color="{Binding Color.ConflictSolverDeletedLineColor, Mode=TwoWay}" /></Border></Popup></StackPanel><Button Content="{Binding Reset}" Command="{Binding Color.ResetDeletedLineCommand}" Grid.Column="3" /></Grid>
                                    <Grid Grid.Row="1" ColumnDefinitions="1*,100,Auto,30"><TextBlock Text="{Binding ConflictSolverImaginaryLineColorText}" TextWrapping="WrapWithOverflow" Margin="0,5,0,0" /><StackPanel Grid.Column="2"><ToggleButton Name="PopupButtonImaginary" Background="Transparent" Width="100" Height="30" Margin="0,0,5,0"><Grid><Rectangle Width="100" Height="{Binding #PopupButtonImaginary.Bounds.Height}"><Rectangle.Fill><VisualBrush TileMode="Tile" SourceRect="0, 0, 16, 16" DestinationRect="0, 0, 16, 16"><VisualBrush.Visual><Path Data="M 0,0 L 0,8 L 8,8 L 8,0 Z M 8,8 L 8,16 L 16,16 L 16,8 Z" Fill="Gray" /></VisualBrush.Visual></VisualBrush></Rectangle.Fill></Rectangle><Rectangle Fill="{Binding Color.ConflictSolverImaginaryLineBrush}" Width="200" Height="200" /></Grid></ToggleButton><Popup IsOpen="{Binding #PopupButtonImaginary.IsChecked, Mode=TwoWay}" StaysOpen="False" PlacementTarget="{Binding #PopupButtonImaginary}"><Border Background="{DynamicResource IronyBackgroundPopupBrush}" Padding="5" CornerRadius="3"><colorPicker:ColorPicker Color="{Binding Color.ConflictSolverImaginaryLineColor, Mode=TwoWay}" /></Border></Popup></StackPanel><Button Content="{Binding Reset}" Command="{Binding Color.ResetImaginaryLineCommand}" Grid.Column="3" /></Grid>
                                    <Grid Grid.Row="2" ColumnDefinitions="1*,100,Auto,30"><TextBlock Text="{Binding ConflictSolverInsertedLineColorText}" TextWrapping="WrapWithOverflow" Margin="0,5,0,0" /><StackPanel Grid.Column="2"><ToggleButton Name="PopupButtonInserted" Background="Transparent" Width="100" Height="30" Margin="0,0,5,0"><Grid><Rectangle Width="100" Height="{Binding #PopupButtonInserted.Bounds.Height}"><Rectangle.Fill><VisualBrush TileMode="Tile" SourceRect="0, 0, 16, 16" DestinationRect="0, 0, 16, 16"><VisualBrush.Visual><Path Data="M 0,0 L 0,8 L 8,8 L 8,0 Z M 8,8 L 8,16 L 16,16 L 16,8 Z" Fill="Gray" /></VisualBrush.Visual></VisualBrush></Rectangle.Fill></Rectangle><Rectangle Fill="{Binding Color.ConflictSolverInsertedLineBrush}" Width="200" Height="200" /></Grid></ToggleButton><Popup IsOpen="{Binding #PopupButtonInserted.IsChecked, Mode=TwoWay}" StaysOpen="False" PlacementTarget="{Binding #PopupButtonInserted}"><Border Background="{DynamicResource IronyBackgroundPopupBrush}" Padding="5" CornerRadius="3"><colorPicker:ColorPicker Color="{Binding Color.ConflictSolverInsertedLineColor, Mode=TwoWay}" /></Border></Popup></StackPanel><Button Content="{Binding Reset}" Command="{Binding Color.ResetInsertedLineCommand}" Grid.Column="3" /></Grid>
                                    <Grid Grid.Row="3" ColumnDefinitions="1*,100,Auto,30"><TextBlock Text="{Binding ConflictSolverModifiedLineColorText}" TextWrapping="WrapWithOverflow" Margin="0,5,0,0" /><StackPanel Grid.Column="2"><ToggleButton Name="PopupButtonModified" Background="Transparent" Width="100" Height="30" Margin="0,0,5,0"><Grid><Rectangle Width="100" Height="{Binding #PopupButtonModified.Bounds.Height}"><Rectangle.Fill><VisualBrush TileMode="Tile" SourceRect="0, 0, 16, 16" DestinationRect="0, 0, 16, 16"><VisualBrush.Visual><Path Data="M 0,0 L 0,8 L 8,8 L 8,0 Z M 8,8 L 8,16 L 16,16 L 16,8 Z" Fill="Gray" /></VisualBrush.Visual></VisualBrush></Rectangle.Fill></Rectangle><Rectangle Fill="{Binding Color.ConflictSolverModifiedLineBrush}" Width="200" Height="200" /></Grid></ToggleButton><Popup IsOpen="{Binding #PopupButtonModified.IsChecked, Mode=TwoWay}" StaysOpen="False" PlacementTarget="{Binding #PopupButtonModified}"><Border Background="{DynamicResource IronyBackgroundPopupBrush}" Padding="5" CornerRadius="3"><colorPicker:ColorPicker Color="{Binding Color.ConflictSolverModifiedLineColor, Mode=TwoWay}" /></Border></Popup></StackPanel><Button Content="{Binding Reset}" Command="{Binding Color.ResetModifiedLineCommand}" Grid.Column="3" /></Grid>
                                </Grid>
                            </StackPanel></ScrollViewer>
                        </TabItem>
                        <TabItem IsVisible="{Binding UpdatesAllowed}">
                            <TabItem.Header><StackPanel Orientation="Horizontal" HorizontalAlignment="Center"><TextBlock Text="{Binding UpdateOptions}" TextWrapping="Wrap" TextAlignment="Center" /><TextBlock Text="●" Foreground="{DynamicResource IronyAccentBrush}" FontSize="18" Margin="5,0,0,0" IsVisible="{Binding UpdateInfoVisible}" /></StackPanel></TabItem.Header>
                            <Grid RowDefinitions="*,Auto" Margin="10">
                                <ScrollViewer Grid.Row="0" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
                                    <StackPanel>
                                        <StackPanel MaxWidth="420" HorizontalAlignment="Left">
                                            <CheckBox Content="{Binding AutoUpdate}" IsChecked="{Binding UpdateSettings.AutoUpdates}" />
                                            <CheckBox Margin="0,5,0,0" Content="{Binding CheckForPrerelease}" IsChecked="{Binding UpdateSettings.CheckForPrerelease}" />
                                            <Button Margin="0,5,0,0" Content="{Binding CheckForUpdates}" Command="{Binding CheckForUpdatesCommand}" MaxHeight="30" />
                                        </StackPanel>
                                        <StackPanel IsVisible="{Binding UpdateInfoVisible}">
                                            <Border Margin="0,10,0,0" BorderThickness="1" BorderBrush="{DynamicResource IronyButtonBorderBrush}"><TextBlock Text="{Binding UpdateReleaseInfo}" FontSize="14" HorizontalAlignment="Center" TextWrapping="Wrap" /></Border>
                                            <html:HtmlLabel Name="changelog" />
                                        </StackPanel>
                                    </StackPanel>
                                </ScrollViewer>
                                <Grid Grid.Row="1" ColumnDefinitions="1*,1*" IsVisible="{Binding UpdateInfoVisible}"><Button Margin="0,5,5,5" Content="{Binding SkipUpdate}" Command="{Binding SkipUpdateCommand}" Grid.Column="0" MaxHeight="30" /><Button Margin="5,5,0,5" Content="{Binding InstallUpdates}" IsVisible="{Binding InstallingUpdatesAllowed}" Command="{Binding InstallUpdatesCommand}" Grid.Column="1" MaxHeight="30" /><Button Margin="5,5,0,5" Content="{Binding OpenReleasePage}" IsVisible="{Binding ManualUpdatesRequired}" Command="{Binding OpenReleasePageCommand}" Grid.Column="1" MaxHeight="30" /></Grid>
                            </Grid>
                        </TabItem>
                    </TabControl>
                    <Button Grid.Row="1" Margin="10" Content="{Binding Close}" Command="{Binding CloseCommand}" HorizontalAlignment="Stretch" MaxWidth="200" HorizontalContentAlignment="Center" />
                </Grid>
            </Border>
        </Popup>
    </Panel>
</UserControl>
